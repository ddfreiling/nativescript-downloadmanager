
<Page navigatingTo="onNavigatingTo" loaded="pageLoaded" xmlns="http://schemas.nativescript.org/tns.xsd" loaded="pageLoaded">
  <ActionBar>
    <Label text="Home" />
  </ActionBar>
  <StackLayout>
    <Label text="{{ message }}" class="message" textWrap="true"/>
    <Button text="Stored Files" tap="onShowStoredFiles"></Button>
    <Button text="Download File" tap="onDownload"></Button>
    <Progress value="{{ progress }}"></Progress>
    <Button text="ShowSpace" tap="onShowSpace"></Button>
    <Label text="{{ spaceAvailable + ' GB space' }}" textWrap="true"/>
    <Label text="{{ 'Status: ' + downloadState }}" textWrap="true" />
  </StackLayout>
</Page>
